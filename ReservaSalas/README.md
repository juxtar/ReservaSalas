# Modelo de Dominio

Dado que el proyecto está desarrollado bajo un diseño orientado a dominio, este assembly contiene todo lo relacionado al modelado del dominio, siendo agnóstico a cuestiones de implementación tales como el acceso a datos.

Por esto es que éste no contiene ninguna referencia hacia ningún otro assembly o librería externa.